CLI improvements:

1. default to a sane model, like opencode
2. allow configuring the default model in e.g. a dotfile
3. winow create should autoprint help
4. should also allow cmd line flags like -p 'prompt here' -a 'agent here', but default to not needing it (since we just need 1 arg, the prompt)
